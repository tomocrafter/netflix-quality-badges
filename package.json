{
  "name": "netflix-quality-badges",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun scripts/build.ts",
    "lint": "oxlint",
    "typecheck": "tsc && tsc -p scripts",
    "check": "bun run lint && bun run typecheck && bun run build"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "oxlint": "^1.83.0",
    "typescript": "^7.0.2"
  }
}
